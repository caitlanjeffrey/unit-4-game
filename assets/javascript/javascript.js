$(document).ready(function(){
    // generate computer random number 
    var computerRandomNumber = Math.floor(Math.random()* 120);

    // generate crystal random numbers
    var crystalOne = 
    var crystalTwo = 
    var crystalThree = 
    var crystalFour = 
});